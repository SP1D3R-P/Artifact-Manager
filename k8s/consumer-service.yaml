apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: poc3
  labels:
    app: consumer
spec:
  type: LoadBalancer
  selector:
    app: consumer
  ports:
  - protocol: TCP
    port: 9000
    targetPort: 9000
    name: http
    nodePort: 30090
