apiVersion: v1
kind: Service
metadata:
  name: builder
  namespace: poc3
  labels:
    app: builder
spec:
  type: LoadBalancer 
  selector:
    app: builder
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http
    nodePort: 30080
